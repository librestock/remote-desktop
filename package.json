{
  "name": "@librestock/remote-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "vitest run",
    "lint": "oxlint src-ui",
    "lint:fix": "oxlint --fix src-ui"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "oxlint": "^1.17.0",
    "vitest": "^4.1.1"
  }
}
